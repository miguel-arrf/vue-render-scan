# Playground

Run the local Vue fixture from the package root:

```sh
npm run build
npm run playground
```

Open the printed local URL, then click the buttons. Components that update should flash with a Vue Render Scan overlay and the toolbar should count redraws.
